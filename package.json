{
  "name": "poll-react-nodejs",
  "version": "1.0.0",
  "description": "$clone $npm i",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "cd server && npm install && npm start",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "engines": {
    "node": "14.16.0"
  },
  "keywords": [],
  "license": "ISC"
}
